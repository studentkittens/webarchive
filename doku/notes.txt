- Wird das editieren alter Stände wirklich gebraucht? Widerspricht das nicht dem sinn eines archivs?
  Sollte es gebraucht werden: Gehen würde es wäre (siehe git_edit_old_commits.txt) 
  Auch: Für das editieren alter stände brauchen wir das commit tag in dem xml tags.
- Soll das xml wirklich mit versioniert werden? Welchen vorteil bringt das? 
  Ohne versionierung dieser hätten wir das problem mit dem synchronisieren der xml dateien nicht.
  Würden andere Probleme sich draus ergeben?
- Wieso Trennung von Analysetool und Client? Klar, Analysetools werden intern aus einen client modul bestehen,
  aber sind damit verschiedene Programme gemeint?
- "Der java client muss sich mit seiner netzwerkadresse anmelden" - wie ist das gemeint?!
- Python 2.7 nur wegen der git library, da es auf subprocess rauflaufen wird, vlt. doch Python3??
- Tagnamen eher: <timeStamp><domain> für leichteres durchsuchen
- Ordnerstruktur vlt.:
  domain/
    voller_name_der_html_datei/
       analyse/
         was_auch_immer_für_datein.xmlpnggif. 
         # pfade sind in metadata.xml verlinkt
       metadata.xml
       data.html #umbenannt 
